@page "/"

<PageTitle>Consumer Client Home</PageTitle>

<h1>CONSUMER CLIENT: 27 May 2026, 06:34 PM</h1>

<h2 class="mt-4">Mikes Pawnshop with Entra Id and APIs using Azure Cosmos Db</h2>

<h3 class="mt-4">Cart and Checkout button display</h3>

<h3 class="mt-4">YARP Auth settings:</h3>
<h4 class="mt-2">Using "AuthorizationPolicy": "anonymous" for Yarp endpoint config  </h4>
<p class="text-danger mt-2">    --- will cause to always omit token.</p>
<p class="text-danger mt-2">    --- causes downstream api to fail as the token is omitted but is required.</p>

<h3 class="mt-4">IDownstreamApi:</h3>
<h4 class="mt-2">Can't use anonymous endpoints with DownstreamApi - Products.Read API and any other anonymous endpoints need standard HttpClient not DownstreamApi.</h4>

<h3 class="mt-4">Cadching:</h3>
<h4 class="mt-2">OutputCache used on Storefront requests for Products GET (Blazor Consumer and Blazor Admin).</h4>
