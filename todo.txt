Admin and Consumer Clients
	when Review order is called 
		need to send AddOrderDTO as an argument and in the Orders.API check that their are items in the order and both billing and shipping addresses
		WORKING ON ACCOUNT STATUS CHECK USING API-KEY AUTH
			OrderService in Orders.API should have dependency InternalAccountsHttpService injected and used to call Accounts.API to check account status before placing order
		ensure if review fails that the order is not placed
		check the cart is emptied after an order is placed successfully
	Need to have constants for account status HOLD or some such and check for that value in OrderService ReviewOrderAsync method

	*** Add Authorize attribute to Accounts.API controllers for public async Task<ActionResult<AccountDTO>> InternalGetByOwnerId() method (temp took off for testing)


Hide/Show elements based on auth
Remove logging of sensitive info

Check Auth in Controllers is uncommented

Add MediatR Behaviors for validation success ??? and any others desired ???

CorrelationId Middleware 
	is double logging http method and path on some calls, e.g. health from client call ???
		_logger.LogCorrelationIdMiddleware(context.Request.Method, context.Request.Path, correlationId, presentInRequestHeader);
		remove method and path - update hi performance log extensions if applicable
		all microservices
	check overall flow from client to apis for both commands and queries
	see and update correlationid_notes.txt

Check layaout Admin Cient - split read v write v ES

Http Responses need a using block

Check all Client Http Client calls log a CorrelationId assigned by client and picked up by API logs.

Update Secrets Requirements in notes

Need buttons in consumer client product detail to add to or remove from cart

Cancellation token: button to cancel in UI

OpenAPI documentation

// Storefront Cart Button Flow - ensure flow is as desired for products list > detail > cart for Admin, Consumer, and Dev Cients

// Add Fluent Validation for API Controller DTOs, or use Data Annotations 
	Products.Write.API
		installed nugets
		updated ValidationExceptionHandler
		updated logic in DevTests ThrowExceptionHandler to throw FluentValidation.ValidationException with two errors enumerated.
		updated GlobalExceptionHandler backup handling of validation exception
		updated GlobalExceptionHandlerTests
		updated ProblemDetailsFactory and StructuredLoggingExceptionHandler (NOT USED FOLDER)
		Added validators for commands 
		Registered validators
		Updated DTOs to remove data annotations
		Added manual validation in controllers
	Products.Read.API
		installed nugets
		updated GlobalExceptionHandlingMiddleware
		updated logic in DevTestsController to throw FluentValidation.ValidationException with two errors enumerated.
		Added validators for commands (ThrowExceptionDtoValidator only)
		Registered validators
		Updated DTOs to remove data annotations
		Added manual validation in controllers

// Logging - Serilog 

// Add HealthChecks 

// MediatR in Products.Write.API

// YARP: Add health checks for downstream services

// Return ProblemDetails in clients if header contains "application/problem+json"

// Caching - YARP for Products Read vs API







